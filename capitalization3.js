const name = "Anastasia"
const capitalize = name.substring(2, 3).toUpperCase();
const lowercase1 = name.substring(0, 2).toLowerCase();
const lowercase2 = name.substring(3).toLowerCase();

console.log(lowercase1+capitalize+lowercase2)